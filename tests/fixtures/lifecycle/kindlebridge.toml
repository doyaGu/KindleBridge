kind = "application"
id = "org.kindlebridge.lifecycle"
version = "0.1.0"
release = 1
channel = "dev"
target = "kindlehf"
firmware_min = [5, 16, 3]
entrypoints = { main = "bin/lifecycle.sh" }
publisher_name = "KindleBridge lifecycle fixture"

[permissions]
requested = []
optional = []

[process]
restart = "never"
stop_timeout_ms = 1000

[data]
policy = "ephemeral"
schema = 1
